<% flash.each do |type, msg| %>
  <div class="alert alert-success" role="notice">
    <%= msg %>
  </div>
<% end %>
<h1>Page Gossips </h1>
<p>Bienvenue sur notre page Gossip No<%= params[:id] %> !! <br> T as vu comment tu veux vour ton potin ?</p>

<p>
  Potin n° <%= Gossip.find(params[:id]).id %><br/>
  Son auteur :<a href="/user/<%= Gossip.find(params[:id]).user.id%>"><%= Gossip.find(params[:id]).user.first_name %></a><br/>
  Sa ville : <a href="/city/<%= User.find(Gossip.find(params[:id]).user.id).city_id%>"><%= User.find(Gossip.find(params[:id]).user.id).city.city %></a><br/>
  Son titre :  <%= Gossip.find(params[:id]).title %><br/>
  Et le gossip : <%= Gossip.find(params[:id]).content %><br/>
  </p>

  <p><a href="<%= edit_gossip_path(Gossip.find(params[:id])) %>">Edit ce Gossip</a></p>
  <p>Voilà, <a href="/home/">bisou</a> !</p>